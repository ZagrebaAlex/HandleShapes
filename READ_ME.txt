applications where the user has the ability to create windows and each window
has its own content we encounter the following scenario. The user does the following
actions:
1. creates a window w1
2. creates within it two squares (r11, r12), two circles (c11,c12), and a triangle
(tr11) (Figure 4-1).
3. Then creates another window w2 that overlaps w1.
4. Creates two squares (r21,r22), two circles (c21,c22), and two triangles within it
(tr21,tr22) (Figure 4-2),
5. He then selects w1 and right-clicks BringToFront. The application brings the
window w1 in front of w2 (Figure
